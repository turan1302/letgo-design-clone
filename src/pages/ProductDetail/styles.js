import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container : {flex : 1,backgroundColor : "white",justifyContent : "space-between"},

  description_area : {paddingHorizontal : 15}
});

export default styles;
